{
  "name": "sneakify",
  "version": "1.0.0",
  "description": "A simple Express.js ecommerce app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm install --prefix ../client && npm run build --prefix ../client"
  },
  "dependencies": {
    "express": "^4.16.4"
  }
}
